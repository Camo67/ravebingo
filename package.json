{
  "name": "ravebingo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"No build step required - static assets only\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
